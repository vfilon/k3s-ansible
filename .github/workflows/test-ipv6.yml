---
name: Test IPv6
on:
  workflow_call:
jobs:
  molecule:
    name: Molecule
    runs-on: macos-12
    strategy:
      matrix:
        scenario:
          - ipv6
      fail-fast: false
    env:
      PYTHON_VERSION: "3.11"
    steps:
      - uses: ./.github/workflows/molecule-template.yml
